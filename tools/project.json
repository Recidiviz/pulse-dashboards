{
  "name": "tools",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "tools",
  "projectType": "library",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["tsc -p ./tsconfig.json"],
        "cwd": "{projectRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/tools"],
      "cache": true,
      "options": {
        "command": "vitest",
        "cwd": "{projectRoot}"
      }
    },
    "search-benchmark-capture-auth": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx src/capture-auth-state.mts",
        "cwd": "{projectRoot}/search-benchmark"
      }
    },
    "search-benchmark-benchmark": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx src/benchmark-search.mts",
        "cwd": "{projectRoot}/search-benchmark"
      }
    },
    "search-benchmark-batch": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx src/run-batch.mts",
        "cwd": "{projectRoot}/search-benchmark"
      }
    }
  },
  "tags": ["scope:server", "type:util"]
}
