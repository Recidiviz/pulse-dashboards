{
  "name": "@jii/paths",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/@jii/paths/src",
  "projectType": "library",
  "tags": ["scope:client"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["tsc -p ./tsconfig.lib.json"],
        "cwd": "{projectRoot}"
      }
    }
  }
}
