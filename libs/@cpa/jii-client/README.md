# Case Planning Assistant - JII Frontend

This library houses all of the JII-facing features for the Case Planning Assistant.

## Running lint and unit tests

Run `nx lint @cpa/jii-client` to execute linting.

Run `nx test @cpa/jii-client` to execute the unit tests via [Vitest](https://vitest.dev/).
