{
  "name": "@cpa/trpc-types",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/@cpa/trpc-types",
  "projectType": "library",
  "tags": ["cpa", "scope:universal"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p ./tsconfig.lib.json",
        "cwd": "{projectRoot}"
      }
    }
  }
}
