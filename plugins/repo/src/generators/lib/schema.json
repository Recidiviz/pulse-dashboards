{
  "$schema": "https://json-schema.org/schema",
  "$id": "Lib",
  "title": "Recidiviz Library",
  "description": "Generates an Nx library using Recidiviz standard configuration.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "x-prompt": "What should the library be named?",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "folder": {
      "type": "string",
      "description": "Optional alternate folder path, relative to /libs",
      "x-prompt": {
        "message": "What should the directory be named? (leave blank to use project name)"
      }
    },
    "libType": {
      "type": "string",
      "description": "Provide the library type",
      "x-prompt": {
        "message": "What type of library should it be?",
        "type": "list",
        "items": [
          { "value": "vanilla", "label": "Vanilla Typescript" },
          { "value": "react", "label": "Typescript with React Components" }
        ]
      }
    },
    "scope": {
      "type": "string",
      "description": "The execution environment (client/server) supported by this library",
      "x-prompt": {
        "message": "Where can this library be used?",
        "type": "list",
        "items": [
          { "value": "client", "label": "Browser only" },
          { "value": "server", "label": "Node environments only" },
          { "value": "universal", "label": "Anywhere" }
        ]
      }
    }
  },
  "required": ["name", "folder", "libType", "scope"]
}
