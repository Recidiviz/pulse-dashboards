{
  "name": "repo",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "plugins/repo/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{workspaceRoot}/coverage/{projectName}"],
      "options": {
        "reportsDirectory": "../../coverage/{projectName}"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "tsc -p ./tsconfig.lib.json --noEmit",
            "forwardAllArgs": false
          }
        ],
        "cwd": "{projectRoot}"
      }
    },
    "manage-contractor-sops": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx src/scripts/contractor-interactive.mts",
        "cwd": "{projectRoot}",
        "env": {
          "NODE_OPTIONS": "--no-warnings"
        }
      }
    },
    "validate-contractor-env-sync": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx src/scripts/validate-contractor-env-sync.mts",
        "cwd": "{projectRoot}"
      }
    }
  }
}
