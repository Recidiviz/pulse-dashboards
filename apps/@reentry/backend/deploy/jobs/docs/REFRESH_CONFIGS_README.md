This job needs to be *re-deployed* before use.
It reads the configs from files and updates the db with their contents.

Deploy
```bash
  cd backend/deploy/jobs
  node deploy_jobs.mjs
````

Then run through the console.
