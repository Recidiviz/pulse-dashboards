# Data fixtures

Unlike data that is generated with some randomness, these fixtures correspond to resident fixture data from the `~datatypes` library, to allow for consistency across apps for integrated features such as the assessment bot.
