# JII Tablet App Import

This is the application responsible for importing data from BigQuery into the Postgres database for the JII tablet app (which is managed via Prisma in the `@jii/prisma` library). It is expected to be deployed as a [Cloud Run Job](https://cloud.google.com/run).
